{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Install APK",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/gradlew.bat",
            "args": ["installDebug"],
            "console": "integratedTerminal",
            "preLaunchTask": "Build Debug APK"
        }
    ]
}