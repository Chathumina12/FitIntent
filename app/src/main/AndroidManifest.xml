<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".FitIntentApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitIntent"
        tools:targetApi="31">

        <!-- Splash Screen Activity -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FitIntent.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Welcome Activity -->
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FitIntent.NoActionBar" />

        <!-- Authentication Activity -->
        <activity
            android:name=".activities.AuthActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- Onboarding Activity -->
        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />

        <!-- Workout Detail Activity -->
        <activity
            android:name=".activities.WorkoutDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Workout Timer Activity -->
        <activity
            android:name=".activities.WorkoutTimerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Edit Profile Activity -->
        <activity
            android:name=".activities.EditProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- Notification Settings Activity -->
        <activity
            android:name=".activities.NotificationSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Onboarding Quiz Activity -->
        <activity
            android:name=".activities.OnboardingQuizActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Notification Receiver -->
        <receiver
            android:name=".utils.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Habit Reminder Receiver -->
        <receiver
            android:name=".utils.HabitReminderReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>